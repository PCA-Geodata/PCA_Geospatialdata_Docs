[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "PCA Geopatial Data department documentation"
authors = [{name = "Pinna V.", email = "vpinna@pre-construct.com"}]
dynamic = ["version", "description"]
